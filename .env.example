# =============================================================
# Percival Deep Research MCP — environment template
# Copy this file to ".env" and fill in the real values.
# Never commit ".env" to version control.
# =============================================================

# OpenAI API key (obrigatória)
OPENAI_API_KEY=sua-chave-openai-aqui

# Brave Search API key para busca web (obrigatória quando RETRIEVER=brave)
BRAVE_API_KEY=sua-chave-brave-aqui
RETRIEVER=brave

# ──────────────────────────────────────────────
# Configuração do GPT Researcher
# ──────────────────────────────────────────────

# Modelo de embedding
EMBEDDING=openai:text-embedding-3-small

# LLM para pesquisa estratégica (use gpt-4o-mini para maior velocidade)
STRATEGIC_LLM=openai:gpt-4o-mini

# Número máximo de iterações de pesquisa (use 2 para maior velocidade)
MAX_ITERATIONS=2

# ──────────────────────────────────────────────
# Configuração do Servidor MCP
# ──────────────────────────────────────────────

# Transporte: stdio (padrão, uso com Nanobot/Claude Desktop) | sse | streamable-http
MCP_TRANSPORT=stdio

# Host e porta (apenas para transporte sse/streamable-http)
MCP_HOST=0.0.0.0
PORT=8000

# Nível de log
LOG_LEVEL=INFO

# ──────────────────────────────────────────────
# Configuração Opcional: Anthropic (Claude)
# ──────────────────────────────────────────────
# ANTHROPIC_API_KEY=sua-chave-anthropic-aqui

# ──────────────────────────────────────────────
# Integração com Nanobot
# ──────────────────────────────────────────────
# Para uso via Nanobot, adicione a seguinte entrada em ~/.nanobot/config.json:
#
# {
#   "mcpServers": {
#     "percival_deep_research": {
#       "command": "uv",
#       "args": [
#         "run",
#         "--no-sync",
#         "percival-deep-research"
#       ],
#       "env": {
#         "OPENAI_API_KEY": "sua-chave-aqui",
#         "OPENAI_BASE_URL": "https://api.venice.ai/api/v1",
#         "FAST_LLM": "venice:llama-3.3-70b",
#         "RETRIEVER": "brave"
#       },
#       "tool_timeout": 300
#     }
#   }
# }